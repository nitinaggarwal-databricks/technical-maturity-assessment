# ===================================
# Databricks Maturity Assessment
# Environment Configuration Template
# ===================================

# ===================================
# DATABASE (REQUIRED)
# ===================================
# PostgreSQL connection string - REQUIRED
# Format: postgresql://username:password@host:port/database
# Example: postgresql://myuser:mypassword@localhost:5432/maturity_assessment
DATABASE_URL=postgresql://user:password@localhost:5432/maturity_assessment

# ===================================
# SERVER
# ===================================
# Port for the API server (default: 5001)
PORT=5001

# Node environment (development | production | test)
NODE_ENV=development

# ===================================
# SESSION & SECURITY
# ===================================
# Session secret for cookie encryption (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your-secure-random-session-secret-here

# CORS allowed origins (comma-separated for multiple origins)
# Example: http://localhost:3000,https://yourdomain.com
CORS_ORIGIN=http://localhost:3000

# ===================================
# EMAIL (OPTIONAL)
# ===================================
# SMTP configuration for sending emails
# If not configured, email features will be disabled
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@yourdomain.com

# ===================================
# OPENAI (OPTIONAL)
# ===================================
# OpenAI API key for AI-powered content generation
# If not configured, AI features will use fallback content
OPENAI_API_KEY=sk-your-openai-api-key-here

# ===================================
# DATA STORAGE (OPTIONAL)
# ===================================
# Custom data directory for file-based backups
# Default: ./data
DATA_DIR=./data

# ===================================
# RAILWAY SPECIFIC (PRODUCTION)
# ===================================
# Railway automatically provides these:
# - DATABASE_URL (PostgreSQL connection)
# - RAILWAY_ENVIRONMENT (production/staging)
# - RAILWAY_PROJECT_ID
# - RAILWAY_SERVICE_ID

# ===================================
# NOTES
# ===================================
# 1. DATABASE_URL is REQUIRED - The app will not start without PostgreSQL
# 2. For local development, set up a PostgreSQL database first
# 3. For Railway deployment, add a PostgreSQL service and DATABASE_URL will be auto-set
# 4. Copy this file to .env and fill in your values
# 5. Generate strong SESSION_SECRET: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

