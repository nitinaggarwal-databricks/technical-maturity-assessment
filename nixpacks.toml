[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = [
  'npm install',
  'cd client && npm install'
]

[phases.build]
cmds = [
  'cd client && CI=false npm run build'
]

[start]
cmd = 'NODE_ENV=production node server/index.js'
