# Environment Configuration for Railway Deployment
# Copy these to your Railway project's environment variables

# SERVER CONFIGURATION
PORT=5000
NODE_ENV=production

# DATABASE (Optional - leave empty for file storage)
DATABASE_URL=

# STORAGE (Point to Railway volume if using persistent storage)
DATA_DIR=/app/data

# OPENAI (Optional - for AI features)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4

# CORS
ALLOWED_ORIGINS=*

# FEATURE FLAGS
ENABLE_OPENAI=false
ENABLE_SAMPLE_GENERATOR=true

